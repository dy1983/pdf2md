# ── 核心 PDF 转换 ──────────────────────────────────────────────
marker-pdf>=0.3.0          # 主转换器（自动下载 OCR/布局模型）
# magic-pdf[full]>=0.11.0  # 备用：MinerU，取消注释即可启用

# ── LLM 接入 ───────────────────────────────────────────────────
openai>=1.30.0             # 兼容 OpenAI / Azure / 本地 LLM (Ollama 等)

# ── 工具库 ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
rich>=13.7.0
tiktoken>=0.7.0            # token 计数（精确切块）
Pillow>=10.0.0             # 图片处理
